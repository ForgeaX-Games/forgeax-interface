// packages/interface/src/core/extensions/foundation-bus.ts
import type { AppExtension } from '../app-shell/types';

export const foundationBusExtension: AppExtension = {
  id: 'foundation.bus', version: '1.0.0', provides: [], setup() {},
};
