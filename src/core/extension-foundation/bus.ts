// Thin re-export shell — implementation lives in
// @forgeax/extension-platform (ADR 0026).
export { EventBus } from '@forgeax/extension-platform';
export type { EventBusOptions } from '@forgeax/extension-platform';
