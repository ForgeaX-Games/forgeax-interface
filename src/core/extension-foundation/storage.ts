// Thin re-export shell — implementation lives in
// @forgeax/extension-platform (ADR 0026).
export { createStorageApi } from '@forgeax/extension-platform';
export type { StorageApi } from '@forgeax/extension-platform';
